#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define CANT 6

int main()
{
    char legajo [CANT]
    int i;

    printf("\nInserte el legajo: ");
    fflush(stdin);
    fgets(legajo,CANT,stdin);

    isdigit(legajo)

    while(isdigit(legajo) == 0){
        printf("\el legajo solo dben ser numeros")


    }
}
